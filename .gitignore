# Nội dung do trang admin lưu khi chạy app.py (dữ liệu chạy thật, không đưa lên repo)
data/

# Thư mục ảnh gốc chưa qua rà soát (chứa cả ảnh có thương hiệu thật của bên
# khác — Mancera, Chanel, Paco Rabanne, R+R Medicinals...). Chỉ các ảnh đã
# được chọn lọc, đổi tên và nén trong imagesP/ mới được đưa lên repo.
Product/

# Mat khau da ma hoa cho HTTP Basic Auth cua trang admin (xem .htaccess).
# KHONG dua len GitHub du la mat khau da bam - repo nay public, ai cung
# tai duoc va co the do offline. Tu upload file nay truc tiep qua File
# Manager/FTP, khong qua Git.
.htpasswd

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Hệ điều hành / IDE
Thumbs.db
Desktop.ini
.DS_Store
.vscode/
.idea/

# File tạm
*.tmp
*.log
